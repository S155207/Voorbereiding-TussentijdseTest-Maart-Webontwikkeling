// const printStuff = (amount: number, text: string):void => {
//     console.log(`Hello ${text}, you are number ${amount}`);
// }
const printStuff = (amount: number, text: string) => console.log(`Hello ${text}, you are number ${amount}`);

// const twoDArray = (element1: string, element2: string): string[] => {
//     return [element1, element2];
// }
const twoDArray = (element1: string, element2: string) => [element1, element2];

// const numberToString = (number: number): string => {
//     return `${number}`;
// }
const numberToString = (number: number) => `${number}`;